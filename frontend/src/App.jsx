export default function App(){
  return (
    <div style={{padding:20}}>
      <h1>Welcome — Symfony + React Hexagonal Skeleton</h1>
      <p>API endpoint example: <code>/api/tasks/{id}</code></p>
    </div>
  )
}
